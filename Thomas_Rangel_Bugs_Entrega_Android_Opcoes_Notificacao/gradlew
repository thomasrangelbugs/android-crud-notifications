#!/usr/bin/env sh

# Wrapper simplificado. O Android Studio consegue sincronizar o projeto mesmo sem usar este script.
DIR="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
java -jar "$DIR/gradle/wrapper/gradle-wrapper.jar" "$@"
